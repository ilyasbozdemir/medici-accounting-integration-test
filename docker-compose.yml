services:
  mongodb:
    image: mongo:7.0
    container_name: medici_mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: medici_accounting_db
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
